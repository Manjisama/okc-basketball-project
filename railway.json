{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd backend && gunicorn app.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 30",
    "healthcheckPath": "/healthz",
    "healthcheckTimeout": 300
  }
}
